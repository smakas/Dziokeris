# Lessons — Džiokeris

_One entry per session. Format: date | what went wrong | fix._

2026-06-05 | Player cards dragged to table went to `G.staging` on click, blocking discard with no escape | Added `playerTableIds` Set; cards dragged from hand to table this turn are tracked and returned directly to hand on click instead of going to staging.
2026-06-05 | Large multi-feature update | Removed staging mechanic entirely; added intra-group combo drag-reorder; suit symbols enlarged; fly animation slowed (0.65s) + glow on newly placed cards; one-step undo (↩); game log with comments in Žurnalas tab (localStorage); combo frequency learning for coach hints; visual dealing animation with deck lift; discard pile shows previous card peeking behind.
2026-07-28 | Cloud Phase 3a (hosting + score tracking): Cloudflare Pages Functions (`functions/api/*.js`) + D1 (`worker/schema.sql`). Verified locally with `npx wrangler pages dev . --persist-to .wrangler/state`. Two gotchas: (1) `wrangler pages dev` must get its D1 binding from `wrangler.toml` — passing `--d1 DB=dziokeris` creates a SEPARATE local DB that `d1 execute` never populated, so tables appear "missing"; drop the flag and rely on wrangler.toml. (2) In a JOIN, `ORDER BY rowid` is ambiguous — qualify it (`rs.rowid`). Client is offline-first: every `fetch('/api/...')` is wrapped so the game still plays with no server.
2026-07-28 | Architecture rework Phase 0–1: split the 1,422-line HTML into a pure engine (`src/engine/`), AI policy (`src/ai/`), and browser controller (`src/ui/app.js`) + `index.html` shell. Engine is deterministic (seeded RNG, `applyAction` reducer, `playerView`) and runs headless in Node. Gotcha found: laying your whole hand (the "nothing to discard" win) must close the round inside the lay/add path, not only on discard — otherwise play falls through and tries to draw again in the play phase. Fixed in `checkLayDownWin`. Old file `www\dziokeris\dziokeris.html` kept live and untouched as fallback until Cloudflare hosting (Phase 3) is ready — deliberately NOT moved yet to avoid breaking the currently-served URL.
2026-07-29 | Feedback doc fixes (5): (1) Round/game end used a blocking modal and hid opponents — replaced with a non-intrusive right-column result panel (`#result-pane`, `showResultPanel`/`togglePanes`) showing per-player round gain + running total + candy pot, and `renderOpponents` now flips opponents' remaining cards face-up with points when `state.roundOver`; board stays visible. (2) A joker on a run reaching the Ace was displayed to the RIGHT of the Ace (looks illegal). Fixed `sortCombo`: leftover jokers extend high only while `14 - highVal` room exists, otherwise go to the LOW side — so ♦9-10-J-Q-K-A+🃏 renders 🃏(=8)-9-10-J-Q-K-A. Display-only; `renderTable` now shows valid groups via `sortCombo`. (3) AI draws now blink the source pile (`.blink` CSS) and show a message for deck draws too ("… paėmė iš malkos"), matching the existing discard message. (4) Removed the duplicate mini-combo strip from opponent panels (`opp{n}cb`) — combos already show on the shared table. (5) Hand drag-reorder placed cards wrong left→right: `handDrop` now picks before/after the target from the pointer's position vs the target's mid-point and recomputes the index after removal, fixing the shift-after-splice off-by-one. Verified all 5 in the in-app browser; 14 engine tests still pass.
2026-06-07 | Table cards couldn't be moved to form new combos: (1) tDrop blocked moves where source would become invalid; (2) tDropNew blocked all table→new-zone drags; (3) tDropFromHand blocked hand card drops onto any group that didn't immediately become valid (e.g. dropping ♥4 onto lone Joker = 2 cards = blocked). Fix: removed all per-drop validation from tDrop, tDropNew, tDropFromHand; all three now allow free placement; hand→new-zone also allowed. Invalid groups glow red, valid glow green. Discard blocked until all groups valid.
